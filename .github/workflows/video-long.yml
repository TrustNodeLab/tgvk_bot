name: Video long (YouTube 16:9)

on:
  workflow_dispatch:
    inputs:
      minutes:
        description: "Длина ролика в минутах (1-40)"
        required: false
        default: "6"
      format:
        description: "Формат: doc / breakdown / top10"
        required: false
        default: "doc"
      profile:
        description: "Редакционный профиль: classic / trustnode_casebook"
        required: false
        default: "classic"
      topic:
        description: "Тема ролика (пусто = по умолчанию)"
        required: false
        default: ""
      script:
        description: "Свой текст/статья (пусто = сценарий пишет LLM)"
        required: false
        default: ""
      from_post:
        description: "Текст поста: GigaChat перепишет его в сценарий (пусто = без рерайта)"
        required: false
        default: ""

jobs:
  video:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_ADMIN_CHAT_ID: ${{ secrets.TELEGRAM_ADMIN_CHAT_ID }}
      VIDEO_PROFILE: ${{ github.event.inputs.profile }}
      LONG_SCRIPT_INPUT: ${{ github.event.inputs.script }}
      LONG_POST_INPUT: ${{ github.event.inputs.from_post }}
    steps:
      - uses: actions/checkout@v5

      - name: Validate source inputs
        run: |
          if [ -n "${LONG_SCRIPT_INPUT:-}" ] && [ -n "${LONG_POST_INPUT:-}" ]; then
            echo "::error::script and from_post are mutually exclusive; provide only one source"
            exit 2
          fi

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt imageio-ffmpeg edge-tts numpy silero

      - name: Validate profile and run offline checks
        id: profile
        run: |
          canonical_profile="$(python - <<'PY'
          import os
          from bot.long_profiles import get_profile
          _, canonical = get_profile(os.environ.get("VIDEO_PROFILE", "classic"))
          print(canonical)
          PY
          )"
          printf 'canonical=%s\n' "$canonical_profile" >> "$GITHUB_OUTPUT"
          python -m unittest tests.test_long_profiles
          python -m unittest tests.test_video_long_profile
          python -m unittest tests.test_digest_video_dispatch

      - name: Start VoiceStudio (XTTS clone ruslan p-2)
        run: |
          # 1) Стековый torch/torchaudio (CPU)
          echo "⏳ Устанавливаем torch 2.5.1 CPU..."
          pip install --quiet torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cpu || {
            echo "❌ не удалось поставить torch CPU"; exit 1
          }
          # 2) TTS 0.22.0 + пины
          echo "⏳ Устанавливаем TTS + зависимости..."
          pip install --quiet TTS==0.22.0 'transformers==4.43.3' imageio-ffmpeg numpy==1.26.4 sherpa-onnx || {
            echo "❌ не удалось поставить TTS/sherpa"; exit 1
          }
          # 3) Скачиваем Piper ruslan (~60MB) — голос-донор для клонирования
          echo "⏳ Скачиваем ru_RU-ruslan-medium (~60MB) как голос-донор..."
          mkdir -p models refs
          curl -sL https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/vits-piper-ru_RU-ruslan-medium.tar.bz2 | tar xj -C models || {
            echo "❌ не удалось скачать модель"; exit 1
          }
          cd models/vits-piper-ru_RU-ruslan-medium
          mv ru_RU-ruslan-medium.onnx model.onnx
          mv ru_RU-ruslan-medium.onnx.json model.onnx.json
          cd ../..
          # 4) Референс голоса: синтез Piper'ом (голос ruslan)
          echo "⏳ Генерируем референс голоса (Piper ruslan)..."
          python - <<'EOF'
          import sherpa_onnx
          cfg = sherpa_onnx.OfflineTtsConfig(
              model=sherpa_onnx.OfflineTtsModelConfig(
                  vits=sherpa_onnx.OfflineTtsVitsModelConfig(
                      model="models/vits-piper-ru_RU-ruslan-medium/model.onnx",
                      tokens="models/vits-piper-ru_RU-ruslan-medium/tokens.txt",
                      lexicon="",
                      data_dir="models/vits-piper-ru_RU-ruslan-medium/espeak-ng-data",
                      dict_dir="",
                  ),
                  num_threads=4,
                  debug=False,
              ),
              rule_fsts="",
              max_num_sentences=1,
          )
          tts = sherpa_onnx.OfflineTts(cfg)
          res = tts.generate("Это голос для клонирования. Он должен звучать естественно и уверенно.", sid=0, speed=0.9)
          import numpy as np, wave
          pcm = (np.asarray(res.samples, dtype=np.float32).clip(-1, 1) * 32767).astype(np.int16)
          with wave.open("refs/ruslan.wav", "wb") as w:
              w.setnchannels(1); w.setsampwidth(2); w.setframerate(tts.sample_rate)
              w.writeframes(pcm.tobytes())
          print("ref bytes:", len(pcm.tobytes()))
          EOF
          ls -la refs/
          # 5) Запускаем XTTS-сервер (клон ruslan, питч -2)
          echo "⏳ Запускаем tts_server_xtts.py..."
          nohup python bot/tts_server_xtts.py --ref-audio refs/ruslan.wav --pitch -2 --speed 0.9 --port 3900 \
            > out_tts_server.log 2>&1 &
          echo "⏳ Ждём HTTP-сервер (XTTS грузится ~1 мин)..."
          for i in $(seq 1 90); do
            if curl -sf http://localhost:3900/health > /dev/null 2>&1; then
              echo "✅ TTS-сервер готов (${i}x5 сек)"
              break
            fi
            sleep 5
          done
          curl -sf http://localhost:3900/health || {
            echo "❌ TTS-сервер не поднялся за 7.5 мин. Лог:"
            cat out_tts_server.log
            exit 1
          }
          echo "⏳ Тест синтеза (короткая фраза)..."
          curl -s -X POST http://localhost:3900/v1/audio/speech \
            -H "Content-Type: application/json" \
            -d '{"model":"tts-1","voice":"alloy","input":"Привет, это тест озвучки","response_format":"wav"}' \
            -o out_tts_test.wav --max-time 180
          ls -la out_tts_test.wav

      - name: Generate long video
        env:
          SCRIPT_TEXT: ${{ github.event.inputs.script }}
          VIDEO_TOPIC: ${{ github.event.inputs.topic }}
          VIDEO_MINUTES: ${{ github.event.inputs.minutes }}
          VIDEO_FORMAT: ${{ github.event.inputs.format }}
          VIDEO_PROFILE: ${{ steps.profile.outputs.canonical }}
          POST_TEXT: ${{ github.event.inputs.from_post }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          LLM_PROVIDER: gigachat
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          ELEVENLABS_VOICE_ID: ${{ secrets.ELEVENLABS_VOICE_ID }}
          TTS_PROVIDER: voicestudio
          VOICESTUDIO_URL: http://localhost:3900/v1
          VOICESTUDIO_VOICE: alloy
          VOICESTUDIO_TIMEOUT: "900"
        run: |
          if [ -n "${SCRIPT_TEXT:-}" ] && [ -n "${POST_TEXT:-}" ]; then
            echo "::error::script and from_post are mutually exclusive; provide only one source"
            exit 2
          fi
          SCRIPT_ARGS=()
          if [ -n "$SCRIPT_TEXT" ]; then
            printf '%s' "$SCRIPT_TEXT" > script.txt
            SCRIPT_ARGS+=(--script-file "script.txt")
          fi
          if [ -n "$POST_TEXT" ]; then
            printf '%s' "$POST_TEXT" > post.txt
            SCRIPT_ARGS+=(--from-post "post.txt")
          fi
          python bot/video_long.py --topic "$VIDEO_TOPIC" \
            --minutes "$VIDEO_MINUTES" \
            --format "$VIDEO_FORMAT" \
            --profile "$VIDEO_PROFILE" \
            --out out/video_long.mp4 \
            --edl-out out/video_long.edl.json \
            --srt-out out/video_long.srt "${SCRIPT_ARGS[@]}"

      - name: Require non-empty SRT
        if: success()
        run: |
          if [ ! -s "out/video_long.srt" ]; then
            echo "::error::Required long SRT is missing or empty: out/video_long.srt"
            exit 1
          fi

      - name: Upload video + EDL + SRT
        uses: actions/upload-artifact@v4
        with:
          name: video-long
          path: |
            out/video_long.mp4
            out/video_long.edl.json
            out/video_long.srt
          retention-days: 14
          if-no-files-found: error

      - name: Verify subtitles sync
        if: success()
        run: |
          if VERIFY_OUTPUT="$(python bot/verify_subs.py out/video_long.mp4 out/video_long.srt 2>&1)"; then
            printf '%s\n' "$VERIFY_OUTPUT"
          else
            status=$?
            printf '%s\n' "$VERIFY_OUTPUT"
            echo "::error::Long subtitle verification failed (exit $status)"
            exit "$status"
          fi
          case "$VERIFY_OUTPUT" in
            *"verify_subs: OK"*) ;;
            *)
              echo "::error::Long subtitle verification skipped or produced no OK result"
              exit 1
              ;;
          esac

      - name: Send video to Telegram
        if: success()
        env:
          VIDEO_MINUTES: ${{ github.event.inputs.minutes }}
          VIDEO_FORMAT: ${{ github.event.inputs.format }}
          SCRIPT_TEXT: ${{ github.event.inputs.script }}
          POST_TEXT: ${{ github.event.inputs.from_post }}
          RUN_ID: ${{ github.run_id }}
        run: |
          CAPTION="🎬 Длинное видео готово ("$VIDEO_MINUTES" мин, "$VIDEO_FORMAT", run "$RUN_ID")"
          if [ -n "$SCRIPT_TEXT" ]; then
            CAPTION+=" — по статье"
          fi
          if [ -n "$POST_TEXT" ]; then
            CAPTION+=" — по посту"
          fi
          # Telegram Bot API: лимит 50MB — сжимаем полный 1080p до <50MB (720p, цикл CRF)
          python - <<'EOF'
          import subprocess, os, sys, shutil
          import imageio_ffmpeg
          ff = imageio_ffmpeg.get_ffmpeg_exe()
          src = "out/video_long.mp4"
          dst = "out/video_long_50.mp4"
          size = os.path.getsize(src)
          print(f"[tg] исходник: {size/1048576:.1f} MB")
          MAX = 48 * 1048576
          if size <= MAX:
              shutil.copyfile(src, dst)
              print("[tg] размер ок, шлём как есть")
              sys.exit(0)
          ok = False
          for crf in (34, 36, 38, 40):
              tmp = f"out/tg_crf{crf}.mp4"
              r = subprocess.run([ff, "-y", "-v", "error", "-i", src,
                                 "-c:v", "libx264", "-preset", "veryfast", "-crf", str(crf),
                                 "-vf", "scale=1280:720", "-c:a", "aac", "-b:a", "96k",
                                 "-movflags", "+faststart", tmp], capture_output=True)
              if r.returncode != 0:
                  print(f"[tg] crf {crf} FAILED: {(r.stderr or b'')[-200:]!r}")
                  continue
              s = os.path.getsize(tmp)
              print(f"[tg] crf {crf}: {s/1048576:.1f} MB")
              if s <= MAX:
                  os.replace(tmp, dst)
                  ok = True
                  break
          if not ok:
              print("::error::не удалось сжать видео до 50MB")
              sys.exit(1)
          EOF
          if [ -f "out/video_long_50.mp4" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendVideo" \
              -F chat_id="${TELEGRAM_ADMIN_CHAT_ID}" \
              -F video="@out/video_long_50.mp4" \
              -F supports_streaming=true \
              -F caption="$CAPTION (сжатая до 50MB для Telegram)"
          else
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendVideo" \
              -F chat_id="${TELEGRAM_ADMIN_CHAT_ID}" \
              -F video="@out/video_long.mp4" \
              -F supports_streaming=true \
              -F caption="$CAPTION"
          fi
          if [ -f "out/video_long.edl.json" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
              -F chat_id="${TELEGRAM_ADMIN_CHAT_ID}" \
              -F document="@out/video_long.edl.json" \
              -F caption="🧩 Монтажный лист (EDL): правь текст/порядок и пересобирай через --edl-in"
          fi

      # Автопубликация long → VK Видео (не валят рендер при ошибке).
      - name: Publish long to VK Video
        if: success()
        continue-on-error: true
        env:
          VK_TOKEN: ${{ secrets.VK_TOKEN }}
          VK_VIDEO_TOKEN: ${{ secrets.VK_VIDEO_TOKEN }}
          VK_GROUP_ID: ${{ secrets.VK_GROUP_ID }}
          VIDEO_TOPIC: ${{ github.event.inputs.topic || 'long video' }}
        run: |
          python -m bot.vk_video out/video_long.mp4 \
            "$VIDEO_TOPIC" || true

      - name: Stop VoiceStudio
        if: always()
        run: |
          echo "=== TTS-сервер лог перед остановкой ==="
          cat out_tts_server.log 2>/dev/null || true
          pkill -f "tts_server" || true

      - name: Notify failure to Telegram
        if: failure()
        env:
          RUN_ID: ${{ github.run_id }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{\"chat_id\":\"${TELEGRAM_ADMIN_CHAT_ID}\",\"text\":\"❌ Длинное видео не собралось (run ${RUN_ID}). Смотри лог в Actions → Video long.\"}"
